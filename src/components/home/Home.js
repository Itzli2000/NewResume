import React, { Fragment } from 'react';
import MainSection from './MainSection';

const Home = () => {
    return (
        <Fragment>
            <MainSection />
        </Fragment>
    );
};

export default Home;